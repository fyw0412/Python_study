1、python中的变量无须先声明再使用，源代码有此痕迹，比如在search（）中id，name的声明没有必要
2、“+”针对的操作数必须是同一种类型，在此项目中表现为int
3、 针对复合型数据结构，比如此项目的中记录学生信息的列表中嵌套字典结构，排序可以采用key=value的形式。
4、文件对象的读取（readlines())返回的是一个字符串列表，对于其中的每一个元素的调用时，要注意数据类型的转化，这里使用的是dict(eval(list))的方法
5、文件对象的写入（write())要注意其接受的参数是字符串格式
